// Firebase is configured via google-services.json (Android)
// and GoogleService-Info.plist (iOS)
// These files are added after Firebase project setup.

// Run: flutterfire configure
// This auto-generates lib/firebase_options.dart

class FirebaseConfig {
  FirebaseConfig._();

  // Push Notification Topics
  static const String topicProcessing = 'processing';
  static const String topicCompleted = 'completed';
}
